/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['img.clerk.com'], // Clerk এর ইমেজ লোডের জন্য অনুমোদিত ডোমেইন
    },
  };
  
  export default nextConfig;
  